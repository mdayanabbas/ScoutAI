import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function JobsPage() {
  return (
    <PlaceholderPage
      title="Jobs"
      description="Job workflows are outside the current frontend foundation scope."
    />
  );
}
